# AGENTS.md

## 项目目标与范围

本项目是一款基于《周易》卦象、并计划逐步加入六爻纳甲能力的跨平台移动应用。必须把以下三类能力分开建模和展示：

1. 简易《周易》卦象解释：本卦、变卦、卦辞爻辞和基础分类解释；
2. 专业六爻纳甲排盘：八宫、世应、纳甲、旺衰、取用神等可追溯规则；
3. 求签式或 AI 文本表达：只能呈现已计算事实，不得反向改变规则结果。

V1 仅包含离线起卦、本卦、变卦、经核验的卦辞爻辞、基础分类解释和历史记录。专业六爻与 AI 属于后续版本。不得在模块一提前实现大规模业务页面或专业算法。

## 指令优先级与目录职责

本文件作用于整个仓库；更深目录中的 `AGENTS.md` 对该目录增加约束，冲突时以更具体且更严格的规则为准。

- `app/`：Expo Router 路由入口，只做导航和顶层 Provider；
- `src/domain/`：纯业务模型与确定性算法；
- `src/application/`：用例、事务边界和端口编排；
- `src/infrastructure/`：SQLite、随机源、传感器、网络与 AI 适配器；
- `src/features/`：页面功能与交互；
- `src/shared/`：主题、组件、错误、日志及通用工具；
- `data/`：通过来源与授权核验的生产文化数据；
- `scripts/`：数据校验和构建脚本；
- `tests/`：单元、集成、固定案例和属性/穷举测试；
- `docs/`：产品、规则、数据、架构、测试与发布决策。

页面不得直接访问 SQLite，必须经过 Repository 和 Application Service。`src/domain` 不得依赖 React、React Native、Expo Router、SQLite、页面组件或平台 API。

## 六爻起卦不变量

- 每次模拟三枚相互独立的铜钱；每枚只能取 2 或 3，且两者概率均为 1/2；
- 三枚相加只能得到 6、7、8、9，其概率必须分别为 1/8、3/8、3/8、1/8；
- 禁止直接把 6、7、8、9 实现为四种等概率随机结果；
- 6 为老阴：阴爻、动爻、变阳；
- 7 为少阳：阳爻、静爻；
- 8 为少阴：阴爻、静爻；
- 9 为老阳：阳爻、动爻、变阴；
- 第一次起爻是最下方的初爻，第六次是最上方的上爻；
- 内部数组统一按初爻到上爻存储，即索引 0 为初爻、索引 5 为上爻；
- 页面可为视觉布局倒序渲染，但不得改变领域数据顺序；
- 每次起爻必须保存三枚铜钱的原始值，不得只保存合计；
- 按钮和摇动只触发起爻；摇动力度不得影响结果；
- 使用可替换的 `RandomSource` 端口。生产使用系统安全随机源，测试注入固定或种子随机源；
- 所有核心函数在相同原始输入和相同 `rulesetVersion` 下必须产生相同结果；
- AI 不得参与随机起卦、卦象映射、基础排盘或吉凶计算。

## 规则版本要求

每个会影响计算的规则集合必须有不可变的 `rulesetVersion`。历史记录同时保存原始铜钱、六爻顺序、使用的规则版本和结构化结果快照。规则升级新增版本，不得覆盖旧记录或静默重算。

八宫、世应、纳甲、六亲、六神、干支历、月建日辰、旬空、生克冲合、旺衰、用神和动变分析在实现前必须记录：规则体系、可靠来源、适用边界、校对人、校对时间和内容版本。来源未确认时停止实现，不得由 Codex 自行选择流派。

## 数据来源限制

- 所有传统文化内容必须满足 `docs/DATA_SOURCES.md` 的元数据要求；
- 数据导入前必须通过 Zod Schema 与语义完整性校验；
- 生产目录只接受 `status: verified` 的数据；
- `pending` 数据只能放在明确隔离的示例/候选区域，且不得进入产品查询；
- 不得用占位文案冒充真实古籍内容，不得批量生成或补全 64 卦、384 爻辞及专业六爻数据；
- 64 卦映射不得依赖数组位置的隐式假设；
- 授权不明按 `licenseStatus: pending` 处理，禁止发布。

## 解卦与 AI 边界

规则引擎先输出结构化事实和命中规则编号，再由模板生成可读文本。不得用随机数生成吉凶。AI 只能进行白话改写、摘要、术语解释和语气变体。

AI 不得修改或虚构：本卦、变卦、动爻、日月干支、世应、用神、旺衰、生克冲合、主象、规则依据或任何确定性计算字段；不得给出医疗诊断、投资保证或法律结论。AI 输出必须经 Schema 校验，并支持本地确定性回退。

## 数据库与迁移

- 所有 SQLite 结构变化使用单向、编号、可重复检查的 migration；
- 禁止在运行时通过散落的 `CREATE TABLE` 隐式改表；
- migration 一旦发布不得修改内容，只能增加后续 migration；
- migration 在事务中执行，记录版本与校验状态；
- Repository 负责数据库映射，domain 不接触 SQLite 类型；
- 破坏性迁移必须先设计备份/转换路径并增加升级测试；
- 原始起卦记录为追加式事实，不因规则升级被覆盖。

## TypeScript 与测试规范

- TypeScript 保持严格模式；禁止无理由使用 `any`、降低类型约束或关闭检查；
- 核心算法必须是可测试的纯 TypeScript，优先纯函数、不可变输入输出和显式类型；
- 不在组件中编写复杂业务判断；公共函数须有明确类型；复杂规则须标注规则编号与来源；
- 禁止通过跳过测试、删除断言、硬编码结果或删除已有功能规避失败；
- 核心算法覆盖正常、边界、错误输入、固定案例及属性/穷举测试；
- 卦象核心实现后必须穷举全部 4096 种六爻状态，验证本卦、变卦、动爻数量和 6/7/8/9 变换不变量；
- 真机传感器和触觉反馈必须真机验证，CI 不得伪造覆盖结论。

常用验证命令：

```bash
pnpm run typecheck
pnpm run lint
pnpm run format:check
pnpm run test
pnpm run validate:data
pnpm run check
```

## 任务执行与报告格式

任务开始前必须：

1. 阅读根目录和作用域内的 `AGENTS.md`、`README.md`、`docs/` 及相关代码；
2. 检查当前实现、Git 状态、运行时和包管理器，不得假设文件或功能不存在；
3. 先报告简明计划、预计修改文件和主要风险，然后直接执行；
4. 只修改当前模块必要文件，保留用户的既有改动。

任务结束必须报告：

1. 修改文件；
2. 新增依赖；
3. 数据库变更；
4. 实际执行命令；
5. 测试结果；
6. 未解决事项；
7. 建议提交信息。

不得声称运行了未运行的命令。不得自动提交、推送、创建远程分支、触发远程构建或删除用户代码，除非用户在当前任务中明确授权。

## 绝对禁止

- 一次性重写已有项目或修改任务范围外文件；
- 凭空选择传统规则体系或补全不可靠数据；
- 用自由文本、AI 或随机结果替代确定性规则计算；
- 把 API 密钥、`.env`、证书、签名材料或用户隐私数据写入仓库；
- 以占位内容、示例数据或被跳过的测试冒充功能完成。

## 已验证部署基线（灵犀 Meta P0）

以下版本组合已完成构建、跨浏览器验收和 Vercel Production 部署。后续升级这些关键版本时，必须重新执行本文件中的质量门禁与线上验收，不得仅凭本地开发服务器结论发布：

- 项目版本：`@liuyao/web@0.1.0`，提交 `e4855d39539959ab2afaf082bda0782fa5426948`；PR：[wanfeng1025/linxi_Meta#1](https://github.com/wanfeng1025/linxi_Meta/pull/1)；
- Web 技术栈：Next.js `16.2.11`、React/React DOM `19.2.3`、GSAP `3.15.0`、TypeScript `6.0.3`、Playwright `1.61.1`（锁文件解析版本）；
- 运行与包管理：Node.js `22.x`（`.node-version` 与 Vercel 项目配置）、pnpm `11.9.0`（`packageManager` 声明）；
- Vercel：项目 `linxi-meta`，Root Directory 为 `apps/web`，框架为 Next.js，Production Node.js 为 `22.x`；部署客户端 Vercel CLI `57.0.0`，远端构建日志使用 Vercel CLI `59.16.0`；
- 已验证部署：Preview `https://linxi-meta-r4dahca5b-wanan3.vercel.app`，Production `https://linxi-meta.vercel.app`，Production deployment `dpl_95v6HqLL222Cua4Tr1KFWkTM5spK`；
- 验收状态：GitHub Stable quality gates、Chromium、Firefox、WebKit、390px mobile 和 axe 流程全部通过；线上 Edge/390px 起卦锁定流程通过；
- 部署边界：Vercel 项目保持公开默认域名，账号、AI 解卦、AR、社区、商城和云端记录仍为预留模块，不得伪称已实现。
